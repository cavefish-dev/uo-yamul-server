import 'package:flutter/material.dart';

class YamulLoading extends StatelessWidget {
  const YamulLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text('Loading'),
    );
  }
}
